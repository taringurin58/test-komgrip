import { createStore } from "vuex";

import dashboardIndex from "./dashboard/index";

export default createStore({
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    dashboardIndex,
  },
});
